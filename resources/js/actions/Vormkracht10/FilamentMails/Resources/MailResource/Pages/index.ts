import ListMails from './ListMails'
import ViewMail from './ViewMail'

const Pages = {
    ListMails: Object.assign(ListMails, ListMails),
    ViewMail: Object.assign(ViewMail, ViewMail),
}

export default Pages