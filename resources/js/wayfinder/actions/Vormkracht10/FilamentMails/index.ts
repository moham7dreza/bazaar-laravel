import Controllers from './Controllers'
import Resources from './Resources'

const FilamentMails = {
    Controllers: Object.assign(Controllers, Controllers),
    Resources: Object.assign(Resources, Resources),
}

export default FilamentMails